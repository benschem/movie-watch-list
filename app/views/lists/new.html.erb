<h1>New List</h1>
<%= simple_form_for(@list) do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.button :submit, :class =>"btn btn-gradient" %>
<% end %>
<%= link_to 'Back to All Lists', root_path, class: "btn btn-gradient" %>
